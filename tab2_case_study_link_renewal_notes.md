# Tab 2 Case-study Link Renewal Notes

The attachment explicitly requires link matching only. **No supplied Google Drive destination may be opened or inspected.** Hyperlinks were extracted locally from the Word document package and matched by their table-row order.

The latest table contains nine students in this order: Rae Liu, Jeremy C, Kelly C, Samson P., Elden Y., Alexander Y., Alfred M., Alexander L., and Billy. Everie and Joel are absent from the latest version and must therefore be removed. Cynthia and Michael were already removed in the previous revision.

All nine rows have a supplied Google Drive file or folder URL. Each `View case study` control must become a safe external link with `target="_blank"` and `rel="noopener noreferrer"`. The existing confidentiality notice must remain unchanged.

Rendered verification confirms the latest nine-student order, exact admissions outcomes, corrected `Jeremy C` and `Kelly C` labels, nine anchor-based `View case study` controls, the confidentiality notice, and unchanged surrounding Tab 2 content. No case-study link was opened during verification.

The nine rendered href values match the nine URLs extracted from the Word document exactly (`9 of 9`). Every anchor includes `target="_blank"` and `rel="noopener noreferrer"`. Mobile and desktop captures confirm the table remains readable and Tabs 1 and 3 remain visually intact. Type checking, the production build, and runtime-log checks passed without errors. No external case-study destination was requested, opened, or inspected.
